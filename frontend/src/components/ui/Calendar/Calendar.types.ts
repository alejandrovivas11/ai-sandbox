import type { DayPickerProps } from 'react-day-picker'

/**
 * Calendar props — a thin wrapper around react-day-picker's DayPicker that
 * applies the design-system token styling.  All DayPicker props are forwarded.
 */
export type CalendarProps = DayPickerProps
